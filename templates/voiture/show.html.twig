{% extends 'base.html.twig' %}

{% block title %}Voiture{% endblock %}

{% block body %}
    <h1>Voiture</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ voiture.id }}</td>
            </tr>
            <tr>
                <th>Matricule</th>
                <td>{{ voiture.matricule }}</td>
            </tr>
            <tr>
                <th>Marque</th>
                <td>{{ voiture.marque }}</td>
            </tr>
            <tr>
                <th>Modele</th>
                <td>{{ voiture.modele }}</td>
            </tr>
            <tr>
                <th>Assurance</th>
                <td>{{ voiture.assurance }}</td>
            </tr>
            <tr>
                <th>VisiteTechnique</th>
                <td>{{ voiture.visiteTechnique ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>CarteGrise</th>
                <td>{{ voiture.carteGrise }}</td>
            </tr>
            <tr>
                <th>PneuSecours</th>
                <td>{{ voiture.pneuSecours ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>MaterielSecours</th>
                <td>{{ voiture.materielSecours ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('voiture_index') }}">back to list</a>

    <a href="{{ path('voiture_edit', {'id': voiture.id}) }}">edit</a>

    {{ include('voiture/_delete_form.html.twig') }}
{% endblock %}
